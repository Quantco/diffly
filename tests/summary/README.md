# Tests for Comparison Summaries

This directory contains tests for summaries of comparisons to visually document
how summaries are displayed and test that they do not change unexpectedly.

To (re)generate the summary outputs, run

```bash
pixi run test -m generate
```
